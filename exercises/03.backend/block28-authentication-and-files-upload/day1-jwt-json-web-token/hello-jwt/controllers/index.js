const ping = require('./ping');
const topSecret = require('./topSecret');
const usersMe = require('./usersMe');
const login = require('./login');
const signup = require('./signup');

module.exports = {
  ping,
  topSecret,
  usersMe,
  login,
  signup,
};
