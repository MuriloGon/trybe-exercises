import chai, {expect} from 'chai';
import chaiHttp from 'chai-http';

describe('atividade 01 - Rota /login', () => {
  it('Deve receber email e senha', () => {
    chai.request('localhost:3000');
  });
});
